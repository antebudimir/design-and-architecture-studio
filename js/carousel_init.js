// vanilla-slider-master
var containerId = 'slider-1';

var options = {
	transitionTime: 500,
	transitionZoom: 'in',
	bullets: true,
	bulletColor: 'yellow',
	arrows: true,
	arrowsHide: true,
	auto: true,
	autoTime: 4000,
};

var slider = createSlider(containerId, options);
